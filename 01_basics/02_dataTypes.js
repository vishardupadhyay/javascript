"use strict" // treat all JS code as newer version.

// alert( 3 + 3 ) // this will throw an error because we are using node.js here not the browser.

let name = "Vishard" // string
let age = 27 // number
let isLoggedIn = false // boolean

// number
// bigint
// string
// boolean
// null => standalone value - it is an object
// undefined
// symbol => uniques
// object

console.log(typeof undefined) // undefined - because it's a type.
console.log(typeof null) // object - because it's an object